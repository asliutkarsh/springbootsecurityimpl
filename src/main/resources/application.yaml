server:
  port: 8080

spring:
  application:
    name: springboot-security-jwt
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/springboot_security_jwt?createDatabaseIfNotExist=true&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false